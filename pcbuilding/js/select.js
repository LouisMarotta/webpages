

function seleziona(ele){
    console.log(ele)

    //Informazioni CPU
    formatocpu=ele.getAttribute("formato")
    nomecpu = ele.getAttribute("nome")

    //Informazioni MOBO
    formatocpu=ele.getAttribute("formato")
    nomecpu = ele.getAttribute("nome")

    //Informazioni RAM
    formatocpu=ele.getAttribute("formato")
    nomecpu = ele.getAttribute("nome")

    //Informazioni Storage
    formatocpu=ele.getAttribute("formato")        
    nomecpu = ele.getAttribute("nome")

    //Informazioni GPU
    formatocpu=ele.getAttribute("formato")        
    nomecpu = ele.getAttribute("nome")

    //Informazioni PSU
    formatocpu=ele.getAttribute("formato")        
    nomecpu = ele.getAttribute("nome")




    document.getElementById("textcpu").innerHTML = nomecpu
    console.log(nomecpu)
    console.log(formatocpu)
}

